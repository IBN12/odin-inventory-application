<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title><%= title %></title>
    </head>

    <body>
        <h1><%= title %></h1>
        <hr />
        <br />

        <section>
            <h2>Games</h2>
            <% games.forEach((game) => { %>
                <div><b>Game:</b> <%= game.name %>; <b>Genre:</b> <%= game.genre %></div>
            <% }) %>
        </section>

    </body>
</html>